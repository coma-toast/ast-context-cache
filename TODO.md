# TODO

## Priority: High

React dashboard migration is complete. Operator UI lives in `ui/` and is served at `/dashboard/`. Legacy HTMX/templ `/partials/*` routes and generated `*_templ.go` HTML components have been removed; Go types used by the JSON API remain under `internal/dashboard/components/`.

## Priority: Medium

## Priority: Low
